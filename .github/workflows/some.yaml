name: some

on:
  push:
    branches:
      - main
      - stage

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: call 1
        uses: ./.github/workflows/common/test-env-share.yaml
      - name: call 2
        uses: ./.github/workflows/common/test-env-share.yaml
  other-job:
    runs-on: ubuntu-latest
    steps:
      - name: call 3
        uses: ./.github/workflows/common/test-env-share.yaml
