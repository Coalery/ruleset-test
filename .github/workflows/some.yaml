name: some

on:
  push:
    branches:
      - main
      - stage

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - name: call 1
        uses: Coalery/ruleset-test/.github/workflows/common/test-env-share.yaml@stage
      - name: call 2
        uses: Coalery/ruleset-test/.github/workflows/common/test-env-share.yaml@stage
  other-job:
    runs-on: ubuntu-latest
    steps:
      - name: call 3
        uses: Coalery/ruleset-test/.github/workflows/common/test-env-share.yaml@stage
